1.  brainstorm variations and alternatives to grid technique for warning system (also think about obstacle persistence)
2.  merge grid obstacle detector to current pipeline
3.  merge grid warning system (if it turns out well) to current pipeline
4.  download and test FastVLM locally
5.  test metric depth models (metric DepthAnythingV2, metric VideoDepthAnything)
6.  in object_detection.py, switch from using "None" everywhere to empty lists
7.  fine-tune sliding window complexity estimation parameters
8.  fine-tune indoor complexity estimation (luminance map) parameters further
9.  fine-tune the grid parameters further, and check if there are more (or better) parameters to add
10. convert YOLO 26 Nano (detect & segment) to tflite and replicate their pre/postprocessing
11. perform some optimizations (find bottlenecks in our code using line_profiler and see if I can optimize/refactor any of them)
12. find similar (or equivalent) libraries, to the ones used here, for android deployment (kotlin/java)
13. add optional debug statements (for later, very low priority)